import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.myanmar.exchange.pos',
  appName: 'Myanmar Exchange',
  webDir: 'dist'
};

export default config;
